#ifndef __K12_INC_0_H__
#define __K12_INC_0_H__

#define Z(x) (sizeof(x))

using C = unsigned char;
using S = size_t;
using I = uint32_t;
using F = double;

#define IF(x,y) {if(x){y;}}

#include <T.h>

#endif
